---

# with no match, default should be used

section:
  set1:
    - MATCH('dummy-target-foo', BREW_TASK_TARGET)
    - a command

  set2:
    - MATCH('dummy-target-bar', BREW_TASK_TARGET)
    - another command

  default:
    - yet another command

kwargs:
  all_commands:
    - and one more

expected:
  default:
    - yet another command
    - and one more
