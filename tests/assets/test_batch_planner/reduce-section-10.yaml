---

# with matching, section reduces to just a few sets

section:
  set1:
    - MATCH('dummy-target', BREW_TASK_TARGET)
    - a command

  set2:
    - MATCH('another-dummy-target', BREW_TASK_TARGET)
    - another command

  set3:
    - MATCH('dummy-target', BREW_TASK_TARGET)
    - completerly different command

  default:
    - yet another command

kwargs:
  all_commands:
    - and one more

expected:
  set1:
    - a command
    - and one more

  set3:
    - completerly different command
    - and one more
