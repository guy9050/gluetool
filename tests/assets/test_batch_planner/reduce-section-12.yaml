---

# flags should work in structured sections

section:
  set1:
    - MATCH('dummy-target', BREW_TASK_TARGET)
    - apply-all: False
    - a command

  set2:
    - MATCH('another-dummy-target', BREW_TASK_TARGET)
    - another command

  set3:
    - MATCH('dummy-target', BREW_TASK_TARGET)
    - options: foo
    - completerly different command

  default:
    - yet another command

kwargs:
  all_commands:
    - and one more

expected:
  set1:
    - a command

  set3:
    - completerly different command foo
    - and one more
