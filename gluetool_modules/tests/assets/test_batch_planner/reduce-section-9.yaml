---

# command sets without flags raise exception

section:
  set1:
    - a command
    - another command

raises:
  klass: gluetool_modules.dispatchers.test_batch_planner.RulesSyntaxError
  match: "Cannot parse rules 'a command': line \\d+, offset \\d+"
