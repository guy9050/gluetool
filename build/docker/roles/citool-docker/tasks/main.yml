---

# install necessary requirements and citool
- import_tasks: install.yml

# this playbook adds citool configuration
- import_tasks: configuration.yml

# this playbook adds ssh key
- import_tasks: ssh.yml

# this playbook adds kerberos keytab and configures kerberos to be container friendly
- import_tasks: kerberos.yml

# this playbook is injected by governing build.yml playbook
- import_tasks: compose-tasks.yml

# clean up tasks
- import_tasks: cleanup.yml
